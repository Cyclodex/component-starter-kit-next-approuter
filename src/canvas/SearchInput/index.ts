import { InputProps } from '@/components/Input';
import { SearchInput } from './SearchInput';

export type SearchInputProps = InputProps;

export const searchInputMappings = {
  searchInput: SearchInput,
};

export default SearchInput;
